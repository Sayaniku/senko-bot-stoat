module.exports = {
    name: "gratuit",
    description: "Petite commande fun qui pourra etre utile pour l'Antre des Renards.",
    execute: async (client, message, args) => {
        const sent = await message.reply(`** **
# G
# R
# A
# T
# U
# I
# T`)
    }
}